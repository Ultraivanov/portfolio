# Command: init-block

Trigger: user writes `init-block <ID>` in chat (e.g. `init-block MVP-01`).

Execute the protocol defined in `.codex/protocols/init-block-protocol.md`.

Do not ask the user any questions before reading that file.
