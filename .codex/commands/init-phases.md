# Command: init-phases

Trigger: user writes `init-phases` in chat.

Execute the protocol defined in `.codex/protocols/init-phases-protocol.md`.

Do not ask the user any questions before reading that file.
